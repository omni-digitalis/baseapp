.pg-password-strength-tip {
  display: flex;
  flex-direction: column;
  padding-top: 14px;

  &-title {
    padding-bottom: 6px;
  }
    
  &-text {
    color: rgb(var(--primary-text-color));
    opacity: 0.72;
    padding-bottom: 2px;
  }
}
