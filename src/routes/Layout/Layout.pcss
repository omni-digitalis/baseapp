.pg-layout {
  background: rgb(var(--main-background-color));
  color: rgb(var(--secondary-contrast-cta-color));
  min-height: calc(100vh - 95px);
  padding-right: 0;
  padding-left: 0;
  padding-top: calc(var(--gap) * 1.3);
}

.trading-layout {
  background: rgb(var(--main-background-color));
}
